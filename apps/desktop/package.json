{
  "name": "@spindeck/desktop",
  "version": "0.1.0",
  "description": "SpinDeck Tauri desktop shell — native WebView UI + invoke IPC",
  "author": "Cherry Fu <https://github.com/dongguacute>",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "lint": "cargo fmt --manifest-path ./src-tauri/Cargo.toml -- --check && cargo clippy --manifest-path ./src-tauri/Cargo.toml -- -D warnings",
    "generate:icons": "node ./scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6"
  }
}
